# A2MCP Router

Invocation or synchronous results -> `invoke.md`. Endpoint deployment,
inspection, or maintenance -> `provider.md`. Route HTTP 402, x402, MPP, and
`paymentId` to the payment protocol.
